name: Build and Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build:
    name: Build on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller

      - name: Build executable
        shell: bash
        run: |
          if [ "$RUNNER_OS" = "Windows" ]; then
            pyinstaller --onefile --windowed main.py -n simple-image-cropper.exe
          elif [ "$RUNNER_OS" = "macOS" ]; then
            pyinstaller --onefile --windowed main.py -n simple-image-cropper-mac
          else
            pyinstaller --onefile --windowed main.py -n simple-image-cropper-linux
          fi

      - name: Create ZIP file (Windows)
        if: runner.os == 'Windows'
        shell: powershell
        run: |
          Compress-Archive -Path dist\simple-image-cropper.exe -DestinationPath simple-image-cropper-windows.zip

      - name: Create ZIP file (macOS)
        if: runner.os == 'macOS'
        shell: bash
        run: |
          zip -r simple-image-cropper-mac.zip dist/simple-image-cropper-mac.app

      - name: Create ZIP file (Linux)
        if: runner.os == 'Linux'
        shell: bash
        run: |
          zip -j simple-image-cropper-linux.zip dist/simple-image-cropper-linux

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: simple-image-cropper-${{ runner.os }}
          path: "*.zip"

  release:
    name: Create GitHub Release
    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          path: dist

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: dist/**/*.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
